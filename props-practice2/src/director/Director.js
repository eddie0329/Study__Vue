import Builder from '../builder/Builder';
import Builder from '../builder/Builder'

export default class Director {
  constructor(h, self) {
    this._builder = new Builder(h, self);
  }

  makeComponents(workType) {
    
  }
}
